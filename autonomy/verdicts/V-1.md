# V-1: gate on the reconciled scaffold (D-ATLAS-SCAFFOLD-RECONCILE-001)

## Verdict: CONTINUE

Subject: `d1aedccd5b6cc2e84bf1358002fd427b3f6c4417`, branch `autonomy/scaffold`, PR
https://github.com/WezzSide/project-atlas/pull/1001 (`-> main`), repo `WezzSide/project-atlas`.
V-0's successor: reconciliation is a distinct, gated event. This verdict authorizes nothing to
merge. `CONTINUE != MERGE AUTHORIZED`, and no cert exists for any head.

### Role-separation exception, recorded not hidden

The same session that authored `d1aedccd` as executor wrote this verdict on the owner's explicit
instruction. That collapses execute and supervise (policy section 2 invariant 6), and
`autonomy/verdicts/**` is in the executor's hard-coded floor, so
`preflight.py scope --role executor` flags this file. To keep the substance of the separation, an
independent supervisor subagent with fresh context gated the same head read-only and returned
CONTINUE with no blocking findings; its reproductions are cited below. The owner should either
ratify this exception or have the next verdict written by a session that did not author the code.

## 1. Lanes: all four green on the subject head

Run https://github.com/WezzSide/project-atlas/actions/runs/35070370123 (`completed`, `success`),
head `d1aedccd`:

| Check | Conclusion | Job |
|---|---|---|
| `control-plane` | success | https://github.com/WezzSide/project-atlas/actions/runs/35070370123/job/104710228779 |
| `quality (ubuntu-latest, 3.12, full)` | success | https://github.com/WezzSide/project-atlas/actions/runs/35070370123/job/104710228859 |
| `quality (ubuntu-latest, 3.13, compat)` | success | https://github.com/WezzSide/project-atlas/actions/runs/35070370123/job/104710228801 |
| `quality (windows-latest, 3.12, windows)` | success | https://github.com/WezzSide/project-atlas/actions/runs/35070370123/job/104710228939 |

These are the four names `policy.md` section 4 `lanes.required` declares. This is the first real
CI signal on the reconciled head: the three earlier runs on this branch were billing-locked and
started no jobs.

## 2. Reconciliation note re-verified against the diff, not the prose

Checked against `git show 84209842:autonomy/tools/preflight.py` (origin parent),
`git show a134ef0b:autonomy/tools/preflight.py` (fork parent) and the file at `d1aedccd`.

- **HALT fail-closed — HOLDS.** `check_git_preflight` calls `refresh_grant_ref` on its first
  line, unconditionally, then reads both kill switches from the returned ref. The independent
  supervisor reproduced it end to end: with a local branch literally named `origin/main` shadowing
  the remote (git itself warns the name is ambiguous), the refresh returned
  `refs/remotes/origin/main` and surfaced `autonomy/HALT exists on refs/remotes/origin/main: the
  loop is stopped`; with the remote removed, both `origin/main` and `refs/remotes/origin/main`
  raise `ConfigError: … a missing remote cannot be treated as a local branch`, including when a
  local `origin/main` branch exists; a failing fetch raises rather than proceeding.
- **`--no-fetch` gone — HOLDS, with a framing correction.** Zero occurrences in the tool; the CLI
  rejects the flag with exit 2; no env var, config or alternate path reaches
  `check_git_preflight` without the refresh, and the `fetch=` keyword the fork retained is gone
  from the signature. **Correction to the note:** its claim 5 ("DROPPED the fork's `--no-fetch`
  CLI flag entirely") overstates what this reconciliation did. The fork had already removed the
  CLI flag at `72910895`, two commits before its own head; what reconciliation removed is the
  residual internal `fetch=` keyword. The end state is stricter than either parent; the wording
  is imprecise and should be corrected when the note is next touched.
- **Shared `_parse_ledger_line` — HOLDS.** Defined at `autonomy/tools/preflight.py:335`, used by
  `load_ledger` (`:361`) and `check_ledger_append` (`:614`). Nine malformed samples (interior
  blank line, missing trailing newline, CR, invalid UTF-8, JSON array, object with no `event`,
  verdict with an invalid verdict name, verdict with a string `iteration`, verdict with a boolean
  `iteration`) plus a valid control were run through both paths: zero disagreements.
- **Nothing lost from either parent.** origin -> head is additive only (`REQUIRED_LANES`/
  `CERT_LANES`, `_parse_ledger_line`); everything else of origin's is byte-identical, including
  `commit_roles` without `--no-merges`, the scope-time refresh, and the normalized
  directive-path check. The two fork tests absent here asserted the fork's rejected resume
  semantics and were replaced by tests asserting the kept semantics.
- **Resume rule.** Verified directly against `check_ledger`: a `STOP` keeps iteration `n` closed
  after a later resume; the resume opens `n+1`; it does not reset the retry cap, un-close a
  `CONTINUE`/`ACCELERATE`/`DEFER` closure, or un-pause an `OWNER_DECISION_REQUIRED` iteration.

## 3. Pin and lane names

At `d1aedccd`, `sha256(autonomy/policy.md)` =
`263bcae0a59fbdc031c0ce0fc81e02e8d8d5d0ecd26fa729d55da26c1dc25767`, identical to
`autonomy/loop.yaml` `policy_sha`. Section 4 `lanes.required` names exactly `control-plane`,
`quality (ubuntu-latest, 3.12, full)`, `quality (ubuntu-latest, 3.13, compat)` and
`quality (windows-latest, 3.12, windows)`, and `lanes.cert_lanes` is `[linux, windows-native]`.

## 4. Preflight still refuses to start an iteration

`python autonomy/tools/preflight.py preflight --iteration 1` exits 1 with exactly
`FAIL missing grant autonomy/grants/G-1.md: no iteration without a grant` / `1 violation(s)`,
run on this head rather than taken from a prior report. No grant exists in this repository.

## 5. Section 12.3 disclosure

At `d1aedccd` the "Not mechanized yet" list still names the verifier/supervisor disagreement
rule, escalation routing, cert-before-packet ordering and forgery detection, the wall-clock and
token budget kill, the green status of the four `lanes.required` checks, the fallback-cert host
and expiry questions, verification of supervisor-issued grants against `autonomy/staging`, and
the section 13 audit. Nothing unmechanized is claimed as done. Cross-checked in code: `BUDGET_KEYS`
carries `max_wall_clock_minutes` and `max_tokens`, but `check_scope` compares only
`max_files_touched` and `max_diff_lines`, so that disclosure is accurate.

Two gaps were found and are **fixed in the follow-up commit named below**, not left open:

1. The "Mechanized so far" paragraph had regressed to the older, shorter wording and no longer
   named the grant-ref refresh, appended-record validation, or resume sequencing, all of which
   are mechanized. Under-disclosure, not an over-claim.
2. Grant signature checking (`require_signed_grants`, `%G? == G`, `preflight.py:554`) is
   implemented but off (`policy.md` `require_signed_grants: false`), and section 12.3 named it in
   neither direction. It is acceptable at level 0, where every grant is owner-committed to `main`
   and anchored by the byte-identity check, and it must be revisited before level 2, when the
   supervisor issues grants itself. Now stated in section 12.3 so it is not inherited silently.

## 6. Scope of the reconciliation diff

`git diff --stat 84209842 d1aedccd` touches only `WORKLOG.md`, `autonomy/**` docs,
`autonomy/loop.yaml` (pin line), `autonomy/policy.md`, `autonomy/tools/preflight.py` and
`tests/unit/test_autonomy_preflight.py`. `autonomy_level`, `MAX_REDESIGN_RETRIES`,
`MAX_AUTONOMY_LEVEL`, `NEVER_WRITABLE`, `EXECUTOR_NEVER`, `NON_EXECUTOR_NEVER`,
`forbidden_scopes`, `role_scopes` and `budget` are unchanged. `lanes.required` grew from two
checks to four, which tightens the gate rather than widening authority.

## 7. Follow-up commit on this PR, and what it re-opens

The section 12.3 fix above changes `policy.md`, so `policy_sha` moves from
`263bcae0…` to `a4e9c18c…`; `loop.yaml` and the three artifacts that quote the pin were updated
in the same commit, and the stale references to the closed PR #946 were repointed to #1001.

That commit becomes a new head, so **this verdict's lane evidence covers `d1aedccd` only**. The
change is documentation plus the pin, with no code change, but the test suite reads `policy.md`
at runtime (the pin tripwire and the lane-name tests), so the three pytest lanes must re-run
before merge: `quality (ubuntu-latest, 3.12, full)`, `quality (ubuntu-latest, 3.13, compat)` and
`quality (windows-latest, 3.12, windows)`. `control-plane` exercises
`atlas-vault-documentation/**`, which this commit does not touch; its green result on `d1aedccd`
remains logically valid, and CI re-runs it anyway. Locally at the new head: ruff clean, and
`tests/unit/test_autonomy_*` 127 passed on Python 3.12 and 3.13.

## 8. Outcome

CONTINUE. No blocking finding. The owner may proceed to draft `G-1` and `D-ATLAS-ITER-1`; merge
remains owner-held and requires the four lanes green on the final head plus an independent
certification. `PROMOTE_ELIGIBLE != MERGED`.
