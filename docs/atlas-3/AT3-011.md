# AT3-011 — File / symbol graph

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `compile_file_graph`
- CLI: `atlas file-graph --vault <vault> --project <id>`
- Declared files/symbols only; does not walk host trees
- Missing declarations stay UNKNOWN
- Path traversal fails closed
- GRAPH != AUTHORITY
- MERGE_AUTHORIZATION = NOT_GRANTED
