# AT3-051 — Independent verification binding

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `bind_independent_verification`
- CLI: `atlas iv-bind --package <id> --candidate-head <sha> --candidate-tree <sha> --observed-head <sha> --observed-tree <sha> --iv-result PASS --verifier <id>`
- Binds IV to one exact HEAD/TREE pair
- IMPLEMENTER != VERIFIER
- Target movement fails closed
- IV != MERGE; certified_for_merge remains false
- MERGE_AUTHORIZATION = NOT_GRANTED
