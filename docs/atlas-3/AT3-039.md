# AT3-039 — Cross-provider conversation normalization

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `normalize_turns`
- Canonical envelope schema: `atlas3.conversation-envelope.v1`
- No new CLI (consumed by AT3-036/037/038 import paths)
- Mixed valid + corrupt turns fail closed (no partial persist)
- Graph is not authority
- Raw transcript is not persisted
- Never writes Truth Core
- MERGE_AUTHORIZATION = NOT_GRANTED
