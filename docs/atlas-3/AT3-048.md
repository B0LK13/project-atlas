# AT3-048 — Unified LLM memory search

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `search_memory`
- Searches extracted items, not transcript dumps
- Cross-project search fails closed
- Mixed valid + corrupt items fail closed
- Never writes Truth Core
- No new search protocol
- MERGE_AUTHORIZATION = NOT_GRANTED
