# AT3-010 — Repository / component inventory

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `compile_inventory`
- CLI: `atlas inventory --vault <vault> --project <id>`
- Honesty: INVENTORY != TRUTH CORE; GRAPH != AUTHORITY
- Missing declared inventory stays UNKNOWN (`NO_DECLARED_INVENTORY`)
- Declared rows require `evidence_refs`
- Foreign-project or authority-claiming fixtures fail closed
- Mixed valid + corrupt declared rows fail closed
- Does not invent host-root or authentic-estate repositories
- MERGE_AUTHORIZATION = NOT_GRANTED
