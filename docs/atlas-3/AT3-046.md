# AT3-046 — Incremental conversation sync

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `apply_local_incremental`
- Capability: `incremental_capability`
- CLI: `atlas memory incremental`
- Local export-cursor incremental apply is implemented
- Live provider incremental sync remains EXTERNAL_BLOCKED
- Native ChatGPT / Claude / Gemini history APIs remain unavailable
- Credentials and `import_mode=API` fail closed
- Mixed valid + corrupt batches fail closed
- Cross-project and conversation mismatches fail closed
- Cursor is last accepted envelope identity, not a wall-clock watermark
- Never writes Truth Core
- MERGE_AUTHORIZATION = NOT_GRANTED
