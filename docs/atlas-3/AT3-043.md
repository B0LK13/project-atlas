# AT3-043 — Conversation decision + intent extraction

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `extract_intent_report`
- CLI: `atlas memory intent --vault <vault> --project <id>`
- Honesty: INTENT != CURRENT STATE; MODEL != OWNER; confirmed owner decisions require `owner_origin`
- Cross-project items fail closed
- Never writes Truth Core
- MERGE_AUTHORIZATION = NOT_GRANTED
