# AT3-060 — Causal graph (CAUSED_BY)

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `compile_causal_graph`
- CLI: `atlas causal-graph --vault <vault> --project <id>`
- Declared CAUSED_BY edges only
- Missing declarations stay UNKNOWN
- Graph != authority
- Provenance required
- MERGE_AUTHORIZATION = NOT_GRANTED
