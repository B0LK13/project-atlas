# AT3-054 — Memory context compiler (consume-only)

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `compile_memory_context`
- Capability: `context_compiler_capability`
- CLI: `atlas memory context`
- Ranks reconciled memory for later certified 2.x context-compiler consumption
- Does **not** rewrite or import the certified 2.x compiler
- Does **not** replace Ask Atlas 2
- Cross-project batches fail closed
- Mixed valid + corrupt items fail closed
- Trust scores / winners / Truth Core promote claims fail closed
- STALE != CURRENT; CURRENT freshness cannot include stale historical memory
- UNKNOWN stays UNKNOWN
- Never writes Truth Core (`write_applied=False`)
- MERGE_AUTHORIZATION = NOT_GRANTED
