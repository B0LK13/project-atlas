# AT3-071 — Transport != authority

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `prove_transport_is_not_authority`
- CLI: `atlas transport-authority --surface <id> --transport-status <code>`
- HTTP 200 / CLI 0 / MCP ok / A2A ack != authority
- Transport success cannot grant truth, merge, security, or owner power
- Unknown surface or owner-power claim fails closed
- MERGE_AUTHORIZATION = NOT_GRANTED
