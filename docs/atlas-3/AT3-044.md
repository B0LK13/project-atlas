# AT3-044 — Memory freshness / invalidation

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `classify_freshness` / `apply_freshness`
- STALE is not CURRENT
- UNKNOWN stays UNKNOWN when stronger evidence is absent
- Historical conversational memory is not silently current truth
- Mixed valid + corrupt items fail closed
- Never writes Truth Core
- No new CLI
- MERGE_AUTHORIZATION = NOT_GRANTED
