# AT3-038 — Gemini fixture ingest

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `import_gemini_export`
- CLI: `atlas memory gemini --export <json> --conversation-id <id>`
- Honesty: `conversation_sync = NOT_IMPLEMENTED`
- `GEMINI.md` is bootstrap, not ingestion (`GEMINI_MD_IS_NOT_INGESTION`)
- Fixtures that claim `live_full_history_sync: true` fail closed
- Mixed valid + corrupt turns fail closed (no partial persist)
- Never writes Truth Core
- MERGE_AUTHORIZATION = NOT_GRANTED
