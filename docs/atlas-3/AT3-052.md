# AT3-052 — ADV binding

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `bind_adversarial_result`
- CLI: `atlas adv-bind --package <id> --candidate-head <sha> --candidate-tree <sha> --observed-head <sha> --observed-tree <sha> --adv-result PASS --adv-id <id>`
- Binds ADV to one exact HEAD/TREE pair
- IMPLEMENTER != ADV
- Target movement fails closed
- ADV != MERGE
- ADV != SECURITY CERTIFICATION
- MERGE_AUTHORIZATION = NOT_GRANTED
