# AT3-100 — Twin health

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `compile_twin_health`
- CLI: `atlas twin-health --vault <dir> --project <id>`
- Derived health signals only; missing stays UNKNOWN
- Health != authority
- Estate availability != owner authorization
- MERGE_AUTHORIZATION = NOT_GRANTED
