# AT3-045 — Provider identity + session lineage

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `build_session_lineage`
- CLI: `atlas memory lineage --vault <vault> --project <id>`
- Same conversation_id cannot change provider
- Same message_id cannot change content_hash
- Cross-project rows fail closed
- MERGE_AUTHORIZATION = NOT_GRANTED
