# AT3-090 — Atlas Home

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `compile_home`
- CLI: `atlas home --vault <dir> --project <id> --budget <n>`
- Composes Pulse + Start + twin health
- UI != canonical truth
- Does not invent a current task
- Budget required (no RAG dump)
- MERGE_AUTHORIZATION = NOT_GRANTED
