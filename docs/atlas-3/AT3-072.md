# AT3-072 — Provider register / capabilities CLI design

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Functions: `compile_provider_register`, `assert_cli_design`
- CLI: `atlas provider-register`
- Design contract only; no CLI proliferation
- Provider register is not a live history API
- `atlas.query.read` and live-sync wrappers are forbidden
- MERGE_AUTHORIZATION = NOT_GRANTED
