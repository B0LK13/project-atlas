# AT3-036 — ChatGPT export ingest

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `import_chatgpt_export`
- CLI: `atlas memory chatgpt --export <path> --conversation-id <id>`
- Honesty: `conversation_sync = NOT_IMPLEMENTED`
- Wraps landed `parse_chat_export`; does not import or replace `chatgpt_bridge`
- Fixtures that claim `live_full_history_sync: true` fail closed
- Mixed valid + corrupt JSON turns fail closed (no partial persist)
- Never writes Truth Core
- MERGE_AUTHORIZATION = NOT_GRANTED
