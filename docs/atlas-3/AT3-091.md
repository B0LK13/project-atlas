# AT3-091 — Timeline

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `compile_timeline`
- CLI: `atlas timeline --vault <dir> --project <id>`
- Orders validated ledger rows by document-declared valid-time
- Wall-clock / observed_at is not valid-time
- Timeline != Truth Core
- MERGE_AUTHORIZATION = NOT_GRANTED
