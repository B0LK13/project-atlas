# AT3-080 — Impact explorer data

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `compile_impact_explorer`
- CLI: `atlas impact-explorer --vault <dir> --project <id>`
- Declared impact rows only; missing stays UNKNOWN
- Graph != authority
- Trust scores and authority winners fail closed
- Does not write AS-GRAPH-003
- MERGE_AUTHORIZATION = NOT_GRANTED
