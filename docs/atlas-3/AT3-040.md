# AT3-040 — Conversation knowledge extractor

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `extract_items`
- Reuses landed ITEM_TYPES
- Deterministic heuristic only; LLM-assisted extraction is not invoked
- Model paraphrase of an owner decision stays `proposed_decision`
- Confirmed owner decisions require explicit `owner_origin`
- Mixed valid + corrupt envelopes fail closed
- Authority is always NON_CANONICAL; never writes Truth Core
- No new CLI
- MERGE_AUTHORIZATION = NOT_GRANTED
