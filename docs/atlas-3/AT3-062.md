# AT3-062 — DECIDED_BY provenance

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `compile_decided_by`
- CLI: `atlas decided-by --vault <vault> --project <id>`
- Declared DECIDED_BY edges only
- Confirmed decisions require explicit owner_origin
- Model/assistant claims fail closed
- Missing declarations stay UNKNOWN
- Graph != authority
- MERGE_AUTHORIZATION = NOT_GRANTED
