# AT3-041 — Cross-LLM deduplication

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `deduplicate_items`
- Exact and near-duplicate detection
- Original provenance is not erased
- Mixed valid + corrupt items fail closed
- Does not collapse state / intent / history layers
- Never writes Truth Core
- No new CLI
- MERGE_AUTHORIZATION = NOT_GRANTED
