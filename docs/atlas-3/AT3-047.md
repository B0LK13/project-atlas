# AT3-047 — Privacy / consent / retention

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Functions: `apply_privacy`, `scan_or_raise`, `privacy_defaults`
- Secret-shaped content fails closed
- Unknown privacy class fails closed
- Raw full-transcript retention is MINIMIZED
- Never writes Truth Core
- No new CLI
- MERGE_AUTHORIZATION = NOT_GRANTED
