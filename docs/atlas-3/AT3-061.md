# AT3-061 — Intent vs current-state honesty wrapper

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `wrap_intent_state_honesty`
- CLI: `atlas memory honesty --vault <vault> --project <id>`
- Composes AT3-043 `extract_intent_report`
- Honesty: INTENT != CURRENT STATE; layers must not collapse; STALE != CURRENT
- Cross-project items fail closed
- Truth Core promotion attempts fail closed
- Never writes Truth Core
- MERGE_AUTHORIZATION = NOT_GRANTED
