# AT3-102 — Provider sync status

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `compile_provider_sync_status`
- Composes AT3-035 connector capabilities and the honest provider matrix
- CLI surface remains `atlas memory providers` / `atlas memory sync` (no new command)
- Live full-history sync remains not implemented
- AT3-046 incremental conversation sync remains EXTERNAL_BLOCKED
- CONNECTED / AVAILABLE is not synchronized
- MERGE_AUTHORIZATION = NOT_GRANTED
