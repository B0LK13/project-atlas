# AT3-082 — Next-action honesty

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `compile_next_action_honesty`
- Composes existing AT3-015 Pulse artifacts and the landed next-lens answer
- Does **not** invoke `compile_pulse()` (Pulse writes generated artifacts)
- CLI surface remains Pulse / next lens (no new command)
- NEXT != command; no auto-execute
- Stale / unverified stay honest (fail closed if presented as current)
- Missing stays UNKNOWN
- Corrupt Pulse / next-lens JSON fails closed
- Graph != authority
- MERGE_AUTHORIZATION = NOT_GRANTED
