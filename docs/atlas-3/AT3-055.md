# AT3-055 — Ranked-context serve (local pack)

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `serve_ranked_context`
- Capability: `context_serve_capability`
- CLI: `atlas memory context --target-provider` (no new top-level command)
- Serves an AT3-054 ranked pack as a local provider-neutral envelope
- Allowed targets: chatgpt, claude, gemini, cursor
- Live provider serve remains EXTERNAL_BLOCKED
- Credentials and native history API claims fail closed
- Cross-project and mixed corrupt inputs fail closed
- Never writes Truth Core
- MERGE_AUTHORIZATION = NOT_GRANTED
