# Ultimate Atlas — Reuse / gap matrix (ULT-00 Step B)

One row per Ultimate subsystem, in wave order. "REUSE" names landed modules
that the wave must build on; "GAP" names what does not exist; "FIRST PACKAGE"
is the smallest additive slice. `OPEN_PR` rows are candidates only —
`OPEN_PR != LANDED`. Test counts are dedicated test files at
`origin/main` `9972d164` unless stated.

| SUBSYSTEM | CURRENT IMPLEMENTATION | CURRENT CONTRACT | TEST COVERAGE | MATURITY | REUSE | GAP | RISK | PROPOSED FIRST PACKAGE |
|---|---|---|---|---|---|---|---|---|
| **Execution Identity** (ULT-01) | Dispatcher observes `base_main`/`candidate_head`/`candidate_tree` from git and strict-compares them; pins travel untyped in `observations.extras` | None named `ExecutionIdentity`; `AgentResultEnvelope` pydantic `extra="forbid"`; `_PIN_RE` 40-hex, `_DIGEST_RE` 64-hex | `test_orchestration_result_binding.py` 15, `test_orchestration_dispatcher.py` 8 | primitives LANDED, contract MISSING | `dispatcher.observe_binding_pins`, `evaluate_identity_binding`, `router.canonical_payload_digest`, `atlas_contracts.versions` patterns, `EvidenceBundle.payload_sha256` | typed contract; `base_tree`; repository field outside a constant; environment/tools/model/agent hashes; one shared canonical serializer | LOW if additive in `atlas3/` + `atlas_contracts/`; MEDIUM if the `extras` channel is changed (orchestration tests) | **ULT-01a** (see FIRST-PACKAGE-ULT-01A.md) |
| **Proof v2 / attestation / proof DAG** (ULT-01) | AT3-050 presence-only 8-slot vector, overwrite per task; AT3-051/052 parameter-only binders | `proof.py:23-32` stage tuple (`foundation.py` asserts it); IV/ADV output dicts with hardcoded `head_match: True` | `test_atlas3_proof_001.py` **2**; `iv_bind_051` 8 (4 negative); `adv_bind_052` 7 (3 negative; `ADV_OBJECT_INVALID` untested) | LANDED_PARTIAL | `PROOF_STAGES`, `write_json_atomic`, `Atlas3Error` codes, IV/ADV fail-closed ladder, `EvidenceBundle` | typed `EvidenceAttestation`; object/identity binding per stage; producer + result digest; proof DAG; v1 compat path; IV/ADV never observe git | LOW: v1 dict path kept; MEDIUM: `foundation.py` and `capabilities.py:81-89` pin proof shape | ULT-01a (attestation + binding); ULT-01b (DAG + observation adapter reusing `observe_binding_pins`) |
| **Program Intelligence** (ULT-02) | Declared-only readers over `generated/ops/atlas3/<area>/<project>/declared.json`; twin constructors pure in-memory | `domain.py` 19 nodes / 15 relationships; `contracts/twin-*.schema.json`; `ALLOWED_IMPACT_KINDS` (4, lowercase) diverges from `TWIN_RELATIONSHIPS` casing | `file_graph_011` 9, `inventory_010` 10, `impact_080` 8, `truth_graph_092` 8, `twin_002` 3 | ISOLATED_RUNTIME (declared) | twin vocabulary, `PROVENANCE_REQUIRED`/`CROSS_PROJECT`/authority-claim refusals, `compile_cache.compute_invalidation_key`, `discovery.py` SHA-256 inventory (frozen, read-only reuse) | any extractor (stdlib `ast` first; Tree-sitter/SCIP as optional adapters per §61); blob/tree-keyed cache; `ProgramIntelligencePort`; `_safe_relpath` does not reject `C:/` or trailing `a/..` | MEDIUM: first module in `atlas3/` that reads a *source tree*; must stay derived, project-scoped, never Layer B | ULT-02a: `ProgramIntelligencePort` + stdlib-`ast` Python extractor producing declared-shape file/symbol rows keyed by blob sha; no new deps |
| **Context Compiler v2 / Start v2** (ULT-03) | `start.py` chars-as-tokens, count-string sections, truncation emitted as UNKNOWN, CURRENT bypass with state lens | 11 `START_SECTIONS`; `FRESHNESS` enum; output written to `generated/ops/atlas3/start/<pid>.json` | `test_atlas3_start_001.py` **4** (no total-budget assertion, no byte-stability assertion, bypass untested) | LANDED_PARTIAL | `runtime_22._reason_included`, overflow `dropped_count`, per-entry provenance, `compat_snapshot_id`; Pulse items with `evidence_refs`; AT3-054/055 | `budget_unit` honesty; `TaskFingerprint`; item-level provenance; `excluded[]` with reasons; tree/compiler-version binding; tokenizer **port** (offline, optional) | LOW–MEDIUM: output shape change for `atlas start` consumers (`home.py` composes it) | ULT-03a: Start honesty fix (unit field, `truncated` vs `UNKNOWN`, carry `evidence_refs`, bind `base_tree` from ULT-01a) before any "v2" |
| **Epistemic / freshness annotation** (ULT-03/14) | `memory/freshness.py` PostgreSQL-only classifier; `authority_class` on events | `AUTHORITY_CLASSES` {derived, observed, non-canonical} | `freshness_044` (memory suite) | LANDED_PARTIAL | freshness state names; STALE != CURRENT tests | `OBSERVED/DERIVED/INFERRED/CONTESTED` field on context items; general freshness rules | LOW | fold into ULT-03a as an additive `epistemic_state` field |
| **Typed Skill Registry** (ULT-04) | Control plane: hash-verified `SKILL.md`, HMAC promotion grants, readiness registry; two incompatible manifest schemas; no index | `skill-manifest.yaml` vs `skills/*/skill.yaml`; `SkillBinding{id,version,sha256}` | control plane 177 tests (outside main lint/type scope) | LANDED_PARTIAL (integrity) / MISSING (registry, eval) | `skill_loader.load` hashing, `readiness.promote` + `authority.issue_grant`, `receipt_gate`, `SkillBinding` | `SkillManifest` v1 schema unifying the two; index; `SkillEvaluation`; fix `version_at_least`; decide which skill is canonical (`receipt_gate.py:27` gates only `atlas-governed-work`) | MEDIUM: touches the governance skill lifecycle; owner must name the canonical skill first | ULT-04a: single `SkillManifest` schema + loader that reads it (control-plane only), no registry semantics change |
| **Agent Genome** (ULT-04) | Three unsynchronized adapter/agent registries; `AgentRecord`/`AgentLease` in autonomy | none | — | MISSING | `AgentLease` fields, `adapter_registry`, `SkillBinding`; open PR #725 `ATLAS_AGENT_REGISTRY_V1` (scripts) | `AgentGenome` contract, hash, compiler | MEDIUM: must not become a fourth registry | after ULT-04a; compose with #725 if merged |
| **Model Router** (ULT-10) | None; `policy.py` routes work to roles; `provider_adapters` stubs `enabled: False`; two hardcoded `DEFAULT_MODEL`s | `PolicyMapping` (fail-closed `UnmappedTransitionError`) | `test_orchestration_policy.py` 19 | MISSING | `policy.py` fail-closed shape, `sdk/cost_guard.py` budgets, `opt_gate` promotion ordering | model registry, `RouteDecision`, explainability, eval binding | LOW now (nothing to break); HIGH later (routing != authority) | not before ULT-10 |
| **Capability Broker** (ULT-05) | `atlas3/capabilities.py` taxonomy (no enforcement, global mutable REGISTRY); `authz.Capability` enforced; `agent_control` levels; `AgentLease` grants paths/caps | `contracts/capability.schema.json` (8 fields); `authz.py:32-47` 14 literals | `capabilities_004` 3; authz + MCP suites; `test_orchestration_autonomy.py` 35 | LANDED_PARTIAL, fragmented | **`AgentLease` + `grant_lease` scope-expansion refusal + `DirectivePermissions`** as the grant substrate; `authz` as the enforcement point; `capabilities.py` as the *definition* layer only | `CapabilityGrant`/`ExecutionToken` contracts; principal; expiry; secret-reference; egress policy object; linkage of the three models | HIGH if `atlas3.capabilities.REGISTRY` is used as the grant store; MEDIUM if built on leases | ULT-05a: `CapabilityGrant` v1 = typed projection of an `AgentLease` bound to an `ExecutionIdentity`; no new enforcement point |
| **MCP gateway / output boundary** (ULT-05) | Read-only zero-arg server; strong input guards; no output scanning/egress/size cap | `mcp_registry` allow-list; `authz` `mcp.read` | `as_2_1_mcp_adv_001` 9, `mcp_brief_001` 7, ~15 read-lens suites | LANDED_PARTIAL | `mcp_server` input ladder, `secrets.scan_text`, `mcp_registry` structural denies | untrusted-output label + `scan_text` on responses; size cap; audit line; `provider-generate` missing from the enable guard (`mcp_registry.py:202-205`) | LOW (additive filter on read path) | ULT-05b: response scanning + `untrusted_data` label on every MCP result; no new tools |
| **ACP / A2A** (ULT-12/13) | none | `surface.py` names | `surface_070` 12, `transport_071` 9 | MISSING | surface/transport != authority provers | adapters | n/a | not before ULT-11 |
| **Execution backends / brain-hands** (ULT-06) | `local_process_transport` (disabled by default, git-diff authority), `local_dispatch_port` (per-attempt worktree, supervisor digest), `SubprocessProcessRunner` | `LocalTaskEnvelope`, `LocalExecutionResult{baseline_sha, changed_paths, violations}` frozen dataclasses; `ProcessRunner` Protocol | `local_process_transport` **43**, `local_dispatch_port` 30, `agent_transport` 7 | LANDED_PARTIAL (T0/T1) | all of the above; `ExecutionHostClass` | `ExecutionBackend` port naming the existing two as `LocalReadOnlyBackend`/`IsolatedWorktreeBackend`; sandbox manifest; resource profile recording; container/gVisor adapters | MEDIUM: orchestration surface, Windows parity required | ULT-06a: port interface + adapters over the two existing backends; **no** new isolation claims |
| **Verifier mesh / critic / breaker** (ULT-07) | Process discipline (GOVERNANCE roles, IV rounds, ADV receipts); AT3-051/052 declaration binders | `iv_bind`/`adv_bind` output dicts | see proof row | LANDED_PARTIAL | AT3-051/052 ladder; `GOVERNANCE.md` roles; open PRs #733 (principal registry), #734 (dispatch), #735 (evidence graph) | verifier classes; principal identity in `src/`; critic; breaker strategy catalog; verification-level policy | HIGH: any runtime "verifier" must not be able to certify; must compose with #733 if merged | after ULT-01a; owner decides #733's fate first |
| **Verification ladder** (ULT-07) | practised, not encoded | none | — | MISSING | WORKLOG seal pattern (targeted → affected → full → controls → IV → exact-head CI) | `verification_level` field on package headers/receipts | LOW | ULT-07a docs-only policy table reconciled with `GOVERNANCE.md` |
| **Advanced testing / impact engine** (ULT-08) | pytest/ruff/mypy; hand-rolled deterministic fuzz; manual mutation controls | none | — | MISSING libs | existing negative-control practice; `test_quarantine_fuzz.py` | `hypothesis` (dependency decision under §61), mutation adapter, impact selector (needs ULT-02) | LOW–MEDIUM (dev-dep addition; Windows CI time) | ULT-08a: owner decision on adding `hypothesis` to `dev` extras; first properties on canonical serialization from ULT-01a |
| **Trajectory summaries / scaling** (ULT-09) | none; handoff + session capture closest | `atlas.coder-alpha.handoff.v1`, `session-capture.v1` | handoff/capture suites | MISSING | `agent_handoff` content-addressing + live freshness recompute; open PR #736 `ATLAS_HANDOFF_V1` | `TrajectorySummary`, store, tournament, early-stop | MEDIUM | not before ULT-03a + ULT-07 |
| **Outcome / eval platform** (ULT-10) | `opt_gate` (hard gates precede score, sealed envelopes), `scoring_broker` out-of-process holdouts, `eval_substrate` roles | `opt-experiment-receipt.schema.json`; `fixtures/eval/opt-gate/thresholds.json` | opt-gate + broker suites | LANDED_PARTIAL | **`decide_promotion` ordering is the template**; broker; `REQUIRED_HARD_GATES` | `EvalCase/Suite/Run`, `OutcomeRecord`, controlled environment profiles (needs ULT-01a environment hash) | MEDIUM | ULT-10a types only, after ULT-01a |
| **Governed multi-hop** (ULT-11) | single hop, read-only task types only | dispatcher docstring `MULTI_HOP_AUTODISPATCH = NOT_IMPLEMENTED` | dispatcher 8 (6 refusal controls), autonomy loop 38 | correctly NOT_IMPLEMENTED | everything in `orchestration/` | — | HIGH by definition | last; unchanged by this reconciliation |
| **Knowledge / memory / failure memory** (ULT-14) | Truth Core, temporal, D-192 memory modules, Pulse, handoffs | many | large | LANDED_PARTIAL | all | episodic + failure memory; graph-guided retrieval | LOW–MEDIUM | after ULT-02/03 |
| **AutoLab** (ULT-15) | wake gate constant CLOSED | opt-gate | opt-gate suites | CONTRACT_ONLY | opt-gate | loop | HIGH | horizon |
| **Observability** (ULT-10/06) | health lenses | none | small | MISSING (OTel) | `ledger_obs`, `twin_health` | trace correlation ids (`run_id` from ULT-01a) | LOW | after ULT-01a |
| **Storage ports / derived cache** (§41–42) | files, JSONL, `compile_cache` | — | — | MISSING (not urgent) | `compile_cache` | blob/tree keyed cache (needs ULT-02) | LOW | ULT-02a |
| **Supply-chain provenance** (§43) | `deps/integrity.json` | — | — | MISSING | — | lock, SBOM | LOW | not scheduled |

## Reused components named (for the return packet)

`knowledge_compiler`, `lineage`, `source_identity`, `bitemporal`/`kdiff`,
`atlas3/events`, `atlas3/ledger`, `atlas3/domain`, `atlas3/twin`,
`atlas3/file_graph`, `atlas3/inventory`, `atlas3/impact`, `atlas3/truth_graph`,
`atlas3/start`, `atlas3/pulse`, `atlas3/proof`, `atlas3/iv_bind`,
`atlas3/adv_bind`, `atlas3/capabilities`, `atlas3/contracts`
(`write_json_atomic`, `Atlas3Error`), `atlas3/memory/*`,
`runtime_22`, `context_pack`, `agent_handoff`, `session_capture`,
`orchestration/dispatcher` (`observe_binding_pins`, `evaluate_identity_binding`),
`orchestration/router` (`canonical_payload_digest`),
`orchestration/local_process_transport`, `orchestration/autonomy`
(`AgentLease`, `grant_lease`, `DirectivePermissions`, `EvidenceBundle`),
`authz`, `mcp_server`/`mcp_registry`, `secrets.scan_text`, `opt_gate`,
`scoring_broker`, `eval_substrate`, `atlas_contracts/*`,
`atlas-vault-documentation/agent_control/*` (skill loader, readiness,
authority grants, receipt gate).

`DUPLICATE_ENGINES_PROPOSED = 0`: no second Truth Core, temporal engine,
identity system, provenance system, event ledger, owner-authority system, or
merge-authority system appears in any "FIRST PACKAGE" cell.
