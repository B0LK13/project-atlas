# AT3-057 — Cursor fixture / local-session ingest

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `import_cursor_export`
- CLI: `atlas memory cursor --export <json> --conversation-id <id>`
- Honesty: `conversation_sync = NOT_IMPLEMENTED`
- Import mode: `LOCAL_SESSION` (structured fixture, not a Cursor Cloud export)
- `AGENTS.md` / `.cursorrules` are bootstrap, not ingestion (`AGENTS_MD_IS_NOT_INGESTION`)
- Fixtures that claim `live_full_history_sync` or `cursor_cloud_history` fail closed
- Mixed valid + corrupt turns fail closed (no partial persist)
- Never writes Truth Core
- MERGE_AUTHORIZATION = NOT_GRANTED
