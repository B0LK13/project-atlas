# AT3-013 — PR / commit / test / build nodes

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `compile_engineering_nodes`
- CLI: `atlas ledger nodes --vault <vault> --project <id>`
- Projects AT3-014 ledger events into derived twin nodes
- Does not invent git history
- Empty ledger stays UNKNOWN
- Ledger corruption fails closed
- GRAPH != AUTHORITY
- MERGE_AUTHORIZATION = NOT_GRANTED
