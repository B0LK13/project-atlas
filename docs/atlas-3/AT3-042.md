# AT3-042 — Cross-LLM conflict detection

Isolated Atlas 3 runtime. Does not mutate certified 2.x surfaces.

- Function: `detect_conflicts`
- Does not collapse state / intent / history
- Does not pick a winner or emit a trust score
- Mixed valid + corrupt items fail closed
- Never writes Truth Core
- No new CLI
- MERGE_AUTHORIZATION = NOT_GRANTED
