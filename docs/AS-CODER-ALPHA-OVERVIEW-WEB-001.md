# AS-CODER-ALPHA-OVERVIEW-WEB-001 — Web `#/overview`

Package ID: `AS-CODER-ALPHA-OVERVIEW-WEB-001`.

Read-only production page for `GET /v1/overview`. Project is explicit
`?project=` only. Live-hook failure stays unknown (`dataSource=null`), never
`demo_stub`.

Honesty: `UI != CANONICAL` / `OVERVIEW != AUTHORITY` / `UNKNOWN != HEALTHY`.
