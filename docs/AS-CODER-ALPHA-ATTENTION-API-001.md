# AS-CODER-ALPHA-ATTENTION-API-001 — read-only GET /v1/attention

Package ID: `AS-CODER-ALPHA-ATTENTION-API-001`.

Projects the existing `atlas attention` / `classify_attention` hygiene lens
to LIVE_API. Distinct from `/v1/project-attention` (AS-2.0 intelligence
risk ranker). Does not invent a second attention engine. Does not execute
commands.

Honesty:

- `ATTENTION != AUTHORITY`
- `HYGIENE != INTELLIGENCE RANK`
- `API != TRUTH CORE`
- `NO IMPLICIT PORTFOLIO-ALL`
- `MERGE_AUTHORIZATION = NOT_GRANTED`
