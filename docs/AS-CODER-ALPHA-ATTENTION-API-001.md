# AS-CODER-ALPHA-ATTENTION-API-001 — read-only GET /v1/attention

Package ID: `AS-CODER-ALPHA-ATTENTION-API-001`.

Projects `atlas attention` / `classify_attention` to LIVE_API. Distinct from
intelligence `GET /v1/project-attention`. Does not write Layer B. CLEAR requires
positive inspection and is never inferred from absence.

Honesty:

- `ATTENTION != AUTHORITY`
- `CLEAR REQUIRES POSITIVE INSPECTION`
- `UNKNOWN != HEALTHY`
- `NO IMPLICIT PORTFOLIO-ALL`
- `MERGE_AUTHORIZATION = NOT_GRANTED`

Web companion: `AS-CODER-ALPHA-ATTENTION-WEB-001` (`#/attention`).
