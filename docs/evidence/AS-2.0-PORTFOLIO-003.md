# AS-2.0-PORTFOLIO-003 — Portfolio Attention Ranking

Discrete attention classes with reasons and evidence.
Sort order is a classification listing, not a priority score.

`numeric_score = None`
`sort_is_score = False`
