# AS-CODER-ALPHA-DOGFOOD-COMPILER-COVERAGE-001

```
PACKAGE = AS-CODER-ALPHA-DOGFOOD-COMPILER-COVERAGE-001
LEASE_ID = LEASE-IMPL-DF-COMPILER-053-A
DIRECTIVE = D-053
DRAFT = YES
CERTIFICATION = NOT_GRANTED
MERGE_AUTHORIZATION = NOT_GRANTED
INDEPENDENT_IV = BLOCKED
SELF_REVIEW != INDEPENDENT_IV
```

DF-038 residual dogfood quality: recall `tech_stack`, `purpose`, and
`important_decisions` from imported Layer A evidence
(`pyproject.toml`, AGENTS, ADR/DECISIONS) without inventing authority.

```
LENS != AUTHORITY
UNKNOWN remains when evidence is absent
README-ONLY / UNSUPPORTED != STACK
SECRET_CONTENT_ECHO = NO
CROSS_PROJECT_LEAK_COUNT = 0
PATH confinement unchanged (resolve_under_root / safe_relative_component)
FABRICATED_FIELDS = 0
```

Overview consumes `attach_compiler_coverage` after source-drift attach.
Brief composition, decisions lens, Next ranking, and Web stay untouched.
