# AS-CODER-ALPHA-STALE-LENS-ADAPTERS-001

```
PACKAGE = AS-CODER-ALPHA-STALE-LENS-ADAPTERS-001
BASE = dc9d81df0ff7106438de44a4bd84df0b955535bc
DIRECTIVE = D-AUTONOMOUS-POST407-DAG-CONTINUATION-044
DRAFT = YES
CERTIFICATION = NOT_GRANTED
MERGE_AUTHORIZATION = NOT_GRANTED
CANONICAL_DRIFT_ENGINE = project_atlas.inventory_drift
```

Thin per-lens honesty hooks over the #407 shared engine. No second
`source_drift_scope.py`. No per-lens hash/scoping engines.

Wired: state, unknown, attention, decisions (path_filter), source-health,
next, changed (unchanged guard), brief (next honesty copy).

Supersede candidates (do not auto-close): #388 #387 #386 #385 #383 #381
plus optional unique hooks from #384 #382.
