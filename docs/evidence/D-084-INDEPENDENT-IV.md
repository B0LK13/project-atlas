# D-084 independent Cloud IV

Estate shape is **not** the unit-test fixture. Names, region labels, and
counts differ (`flood-bin` / `omega-hold` / `pair-dock` / `shell-box` /
`dual-git`, 600 flood clones, cap 35).

```
CLOUD_IV = PASS
candidate_selection_policy = deterministic_hierarchical_fair_v2
project_candidates_seen = 610
project_candidates_preselected = 105
project_candidates_enriched = 105
project_candidates_emitted = 11
EXPENSIVE_ENRICHMENT_FOR_ALL_SIGHTINGS = NO
SELECTED_PROJECT_SET identical under name_asc / name_desc = YES
NOISY_REGION_MONOPOLY = NO   # flood-bin emitted 2 of 35
omega-hold keepers = 4/4
pair-dock siblings = dock-left + dock-right
shell-root preserved; inner-gadget did not displace (tiny cap=1 proof)
outer-hold + inner-hold both eligible
```

Machine receipt: `/opt/cursor/artifacts/d084_cloud_iv.json`

This is not an authentic `D:\` result and is not a runtime claim.
