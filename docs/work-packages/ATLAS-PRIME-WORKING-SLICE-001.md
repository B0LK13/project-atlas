# MISSION = ATLAS-PRIME-WORKING-SLICE-001

Owner-issued mission text (verbatim, authoritative contract for this goal).
Issued through the trusted operator route on 2026-09-15.

## GOAL

Deliver one real, bounded Prime development cycle through Atlas:

supervisor dispatch
→ model-issued ipython call
→ successful kernel execution
→ useful code change
→ passing tests
→ automatic review dispatch
→ independent verdict and receipt.

A report, fixture, failing toolResult, or coding result produced by
another executor does not satisfy the Prime-execution requirement.

## OWNER AUTHORIZATION

This instruction becomes effective when I issue it through the trusted
operator route.

Authorize this new mission, its task admissions, and the necessary
executor assignments. Do not revive expired grants.

Limits:
- Four hours maximum from admission; no automatic extension.
- One implementation codingworker.
- One Prime parent with at most one direct child.
- At most two independent reviewer launches.
- Implementation: 500,000 input/output tokens maximum.
- Review: 100,000 input/output tokens maximum, reserved before coding.
- Local Prime execution: 16 inference requests, 80,000 input/output
  tokens and 12,000 output tokens maximum, including children/retries.
- Preserve at least 2 GiB available host RAM and the existing 6 GiB
  combined pilot memory cap.

Use only the existing authenticated Codex route and the installed
Qwen3 1.7B model digest, within available account capacity. No purchases,
paid overage, new providers, credential imports, or model downloads.

Preserve previous usage. Stronger resource/account restrictions and
explicit stops remain binding. Register the absolute expiry and the
implementation → local validation → review taskgraph before dispatch.

## 1. REUSE THE WORK; DO NOT REPEAT THE AUDIT

Preserve the diagnostic CERTIFIED result and historical failed attempts.
Read the canonical bindings once.

ff3075f9 is a documentation pin, not automatically the executable
candidate. Bind the actual source, runtime, configuration and evidence.

Reuse the existing verification.py and schema-repair implementation
where present. Coordinate with the other lane before changing shared
files. An archived handoff entry does not authorize deleting or
rebuilding another lane's implementation.

No rebootstrap, duplicate scheduler, registry reset or state reset.

## 2. REPAIR THE KNOWN KERNEL FAILURE FIRST

Use capability-1's recorded valid toolcall and kernel-start error as
the concrete repair target. Do not reinterpret it as successful
tool execution or as proof that the model can never call tools.

Correct PRIME_AGENT_KERNEL_PYTHON and the compatible runtime binding
in the environment actually used by the dispatched Prime worker.

Before any new inference:
- Validate the selected interpreter and runtime compatibility.
- Start the kernel through the real host/launcher/sandbox path.
- Execute a harmless operator-authored cell and receive its result.
- Verify cleanup and the existing resource/security boundaries.
- Add the focused regression test for the original binding failure.

Label these checks model-free. A convenient Python environment outside
the actual worker path is not sufficient.

Reuse verified runtime artifacts in a mission-owned environment.
No silent fallback to personal environments or unpinned upgrades.

## 3. MAKE THE REVIEW PATH READY BEFORE THE PILOT

Where the verifier schema defect remains unresolved, repair the actual
submitted schema using the saved rejection and existing
ATLAS-REVIEW-SCHEMA-REPAIR-001 work.

Reuse the built verification-only route. Do not build another handler.

Test serialization, malformed/partial responses, FAIL, UNKNOWN and
candidate/schema binding offline. Do not remove strictness, use a
free-text fallback, or manufacture PASS.

Ensure the supervisor can admit and dispatch the review phase using
the reserved allocation. Do not consume a live review launch merely
to test the schema.

Both kernel preflight and local review-contract checks must pass
before the local model trial starts.

## 4. HAVE PRIME COMPLETE ONE USEFUL TASK

Explicitly admit this bounded coding task:

Expose actionable kernel-readiness failures in the existing A1 status
projection and add a regression test for that behavior.

Reserve this task for Prime. The setup codingworker must not complete
it first and then present its patch as Prime output.

If that behavior already exists, select one evidenced missing regression
in the same feature. Do not introduce artificial changes just to satisfy
GIT_TREE_CHANGED.

Use the installed model/configuration after the demonstrated environment
repair. This authorizes a changed-condition trial, not a repeat of the
same failed capability audit.

Require:
- A model-originated, structured ipython call.
- Successful execution in the actual Prime kernel.
- A genuine task-scoped patch.
- Tests run against that patch.
- A result collected by the Atlas supervisor.

One admitted read-only child may inspect the patch. Its result does
not replace the independent verifier.

Capture the offered tool schema/request evidence without secrets,
provider toolcall, kernel result, task diff and test output.
Missing or reset responses remain UNKNOWN.

Stop repeated identical failures. No model roulette or acceptance
downgrade. Continue independent model-free work where still useful
and authorized.

## 5. INTEGRATE AND REVIEW THE EXACT RESULT

Use isolated worktrees and explicit mutation paths for the necessary
kernel/launcher, review schema, A1 projection and tests.

The exact historical manifest delta may remain in this development
candidate, with its original scope violation recorded. This prospective
retention decision does not rewrite its original authorization history.

Do not overwrite parallel WIP or hotpatch runtime-008.
No publication, merge, shared-service replacement or production action.

Freeze the actual code/delta, runtime, configuration, review schema
and acceptance criteria.

Have the supervisor automatically dispatch a genuine non-author reviewer.
The reviewer must not have authored the changes being reviewed.

A second reviewer launch is permitted only after a concrete correction
following the first review, within this mission's remaining allocation.
Do not repeat the same failed launch unchanged.

Validate and publish the genuine verdict through the trusted route,
then read back the receipt and subject binding.

Keep historical CERTIFIED and UNKNOWN receipts separate. A new review
does not erase history, and a PASS does not close unrelated human,
CI, Vault or deployment gates.

## 6. FINISH ON EXECUTION EVIDENCE

Continue admitted implementation, testing, repair and review work
without requesting routine confirmations.

Stop only on:
- Verified delivery of this mission.
- An explicit stop.
- A hard resource/budget limit.
- A specific remaining technical or access barrier.

Report once:
- Exact candidate and runtime identities.
- Kernel repair and focused regression evidence.
- Real Prime tool execution.
- Prime-produced patch and test results.
- The actual automatic transition to review.
- Independent verdict and receipt-readback.
- Cumulative usage and remaining external gates.

If the local model still cannot complete the task after the runtime
repair, report the observed failure and the evidence boundary.
Do not present setup tests, fixtures or another executor's work as
successful Prime development.

Begin with the kernel repair and review-path readiness.
Do not respond with another handoff audit or a plan as the only result.
