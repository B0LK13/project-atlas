# AS-CODER-ALPHA-DECISIONS-API-001 — read-only GET /v1/decisions

Package ID: `AS-CODER-ALPHA-DECISIONS-API-001`.

Projects the existing `atlas decisions` / `build_decisions_lens` to LIVE_API.
Empty evidence stays UNKNOWN. Does not invent ACTIVE_GOVERNING authority.

Honesty:

- `DECISIONS != AUTHORITY`
- `LENS != LAYER B`
- `API != TRUTH CORE`
- `NO IMPLICIT PORTFOLIO-ALL`
- `MERGE_AUTHORIZATION = NOT_GRANTED`
