# AS-CODER-ALPHA-DISCOVERY-MCP-001 — vault-scoped estate discovery

Zero-arg MCP consume of the existing LIVE_API `GET /v1/discovery` projection.

Package ID: `AS-CODER-ALPHA-DISCOVERY-MCP-001`.

## Honesty

- Discover ≠ ingest ≠ trust ≠ authority
- Absent or malformed report invents no PILOT roots
- `owner_capability_granted` is hardcoded false
- MCP ≠ authority
- No request-arg protocol

Web `#/discovery` already exists and is unchanged.
