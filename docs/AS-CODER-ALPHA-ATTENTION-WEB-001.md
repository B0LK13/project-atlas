# AS-CODER-ALPHA-ATTENTION-WEB-001 — Web `#/attention`

Package ID: `AS-CODER-ALPHA-ATTENTION-WEB-001`.

Read-only production page for `GET /v1/attention` (Coder Alpha hygiene).
This is not the intelligence `/v1/project-attention` page. Project is explicit
`?project=` only.

Honesty: `UI != CANONICAL` / `ATTENTION != AUTHORITY` / `CLEAR != DEFAULT`.
