# AS-CODER-ALPHA-DECISIONS-WEB-001 — Web `#/decisions`

Package ID: `AS-CODER-ALPHA-DECISIONS-WEB-001`.

Read-only production page for `GET /v1/decisions`. Project is explicit
`?project=` only. Decision rows are observations, not owner grants.

Honesty: `UI != CANONICAL` / `DECISIONS != AUTHORITY` / `ACTIVE_GOVERNING != TRUST`.
