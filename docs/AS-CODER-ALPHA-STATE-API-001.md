# AS-CODER-ALPHA-STATE-API-001 — read-only GET /v1/state

Package ID: `AS-CODER-ALPHA-STATE-API-001`.

Projects `atlas state` / `build_state_lens` to LIVE_API. Does not write Layer B
or `generated/answers`. Distinct from intelligence `GET /v1/project-state`.

Honesty:

- `STATE != AUTHORITY`
- `ROLLUP != TRUST SCORE`
- `UNKNOWN != HEALTHY`
- `NO IMPLICIT PORTFOLIO-ALL`
- `MERGE_AUTHORIZATION = NOT_GRANTED`

Web companion: `AS-CODER-ALPHA-STATE-WEB-001` (`#/state`).
