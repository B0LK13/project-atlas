# AS-CODER-ALPHA-OVERVIEW-API-001 — read-only GET /v1/overview

Package ID: `AS-CODER-ALPHA-OVERVIEW-API-001`.

Projects the existing `atlas overview` / `build_overview_lens` to LIVE_API.
Does not write overview answers. Missing `project.md` stays UNKNOWN.

Honesty:

- `OVERVIEW != AUTHORITY`
- `LENS != LAYER B`
- `API != TRUTH CORE`
- `NO IMPLICIT PORTFOLIO-ALL`
- `MERGE_AUTHORIZATION = NOT_GRANTED`
