# AS-CODER-ALPHA-STATE-ATTENTION-WEB-MCP-001

First-class Current State and Attention surfaces on current main.

Package ID: `AS-CODER-ALPHA-STATE-ATTENTION-WEB-MCP-001`.

## What this is

Read-only Coder Alpha journey pages and vault-scoped MCP tools over the
LIVE_API projections that already exist on main:

- `GET /v1/project-state?project=`
- `GET /v1/project-attention?project=`

Web routes:

- `/state?project=`
- `/attention?project=`

MCP tools (zero-arg, vault-scoped, no request project selector):

- `atlas.project-state.read`
- `atlas.project-attention.read`

## Honesty

- `UI != CANONICAL TRUTH`
- `DERIVED STATE != AUTHORITY`
- `ATTENTION != SCORE`
- `RISK != FACT`
- `UNKNOWN != HEALTHY`
- `MCP != AUTHORITY`
- Vault-scoped read is not implicit portfolio-all
- Request args / write / path keys remain forbidden

## What this is not

- Not D-149 / authentic-estate owner-gate work
- Not `/v1/inbox`, `/v1/overview`, `/v1/decisions`, `/v1/unknown`, `/v1/changed`, `/v1/next`
- Not a new LIVE_API route
- Not merge authorization
- Not authentic O2 certification
