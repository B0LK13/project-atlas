# AS-CODER-ALPHA-NEXT-API-001

Read-only LIVE_API projection of `atlas next`.

```
GET /v1/next?project=<id>
```

- Project scope required. No implicit portfolio-all.
- NEXT LENS != AUTHORITY.
- NEXT ACTION != COMMAND.
- UNKNOWN is valid and is not rewritten as healthy work.
- No Layer B writes. Does not materialize `generated/answers/`.
- Same project-token boundary as `/v1/source-health` (`^[a-z][a-z0-9-]{0,63}$`).
- Not `AS-2.0-NEXT-001` / Wave 15-16 intelligence.

This is a current-main reconstruction of the stale conflicting draft #390.
#390 is not mutated. This package does not touch #402 / #394 / #396.
