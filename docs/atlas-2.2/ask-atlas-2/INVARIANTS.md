# Ask Atlas 2 deepen — hard invariants (PREP)

Package: **AS-2.2-ASK2-DEEPEN-PREP-001**  
Status: **normative for this PREP tree**; runtime enforcement deferred until unlock.

## 1. Ask2 ≠ live mutate

| Signal | Allowed | Forbidden |
|---|---|---|
| Deepen docs / stubs | New files under `ask-atlas-2/` | Edit `ask_atlas_live.py` |
| 2.1 LIVE_READ_ONLY path | Document as adjacent substrate | Replace / reopen / dual-own |
| Live match answers | Cite as conceptual peer | Stamp deepen answers as live |

Any consumer that mutates the 2.1 live path from this PREP lane is
**out of contract**.

## 2. UI ≠ canonical

| Surface | May do | Must not do |
|---|---|---|
| Web / MCP / CLI lens | Render deepen fields | Write Layer B / claims |
| Deepen answer view | Project pack → answer | `_promote` / canonical write |
| Ask Atlas CONFLICTS slot | Cite retained conflicts | Stamp authority winners |

Truth boundary string (prep):  
`ASK ATLAS 2 DEEPEN ≠ LIVE MUTATE / ≠ CANONICAL WRITE / ≠ AUTHORITY / UI ≠ TRUTH`

## 3. LLM ≠ authority

| Field | Const / rule |
|---|---|
| `authority.level` on envelopes | `derived` |
| `llm_authority` | `false` const |
| Subjective confidence / trust | **forbidden** (objective signals only) |
| LLM-stamped ANSWER as truth | rejected (`llm_authority_stamp`) |

## 4. No runtime `ask_atlas_live` mutation in PREP

This PREP lane **must not** edit:

- `src/project_atlas/ask_atlas_live.py`
- `src/project_atlas/web_ask_atlas.py`
- shipped package schemas for Ask / web-ask contracts
- research stubs under `docs/atlas-2.2/contracts/research/` (peer ownership)

Consume-only references to the 2.1 live path are documentation links, not
code ownership.

## 5. no PILOT invent / certification wall

| Field | Const / rule |
|---|---|
| `pilot_roots` | `0` |
| `authentic_estate` | `false` on fixtures |
| `evidence_class` | `fixture-only` |
| `ATLAS_2_1_RELEASE_CERTIFIED` | **NO** |
| `ATLAS_2_2_INTELLIGENCE_IMPLEMENTATION_UNLOCKED` | **NO** |

Fixture deepen rehearsal ≠ authentic estate PILOT PASSED ≠ 2.1 RELEASE
CERTIFIED ≠ 2.2 unlock.
