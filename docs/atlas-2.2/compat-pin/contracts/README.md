# Compatibility pin — contract stubs (docs-owned)

Package: **AS-2.2-COMPAT-PIN-PREP-001**

These JSON Schema files are **PREP STUBS** under `docs/atlas-2.2/compat-pin/contracts/`.
They are **not** shipped as `project_atlas` package data on this tip.

| Schema | File |
|---|---|
| Expectation inventory | `compat-pin-expectation.schema.json` |
| Scenario row | `compat-pin-scenario.schema.json` |

See [`../CONTRACT.md`](../CONTRACT.md) for requirement traceability.
