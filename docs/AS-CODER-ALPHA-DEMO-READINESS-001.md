# AS-CODER-ALPHA-DEMO-READINESS-001

End-to-end Coder Alpha demo acceptance over the Harbor fixture plus a decoy project.

```
DEMO_FIXTURE != AUTHENTIC_PILOT
DEMO != RELEASE
UI != CANONICAL_TRUTH
MODEL_OUTPUT != AUTHORITY
```

Exercises connect, overview, state, changed, decisions, unknown, attention,
source-health, next, brief, context, handoff, and Obsidian projection via
library APIs (not a Web browser). Web read-lens presence is not claimed as
canonical truth.

Never emits `RELEASE` or `COMMERCIAL_GA`.
