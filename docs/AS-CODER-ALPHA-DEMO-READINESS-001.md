# AS-CODER-ALPHA-DEMO-READINESS-001

Current-main reconstruction of historical #369 demo readiness.

This is a derived journey classifier, not a second truth engine.

Journey (each stage is READY | PARTIAL | BLOCKED | UNKNOWN | NOT_IMPLEMENTED):

1. PROJECT ROOT
2. CONNECT / DISCOVER
3. PROJECT IDENTITY
4. SOURCE INVENTORY
5. DRIFT STATE (consumes `inventory_drift`; does not duplicate it)
6. OVERVIEW
7. ARCHITECTURE
8. NEXT (lens/CLI landed; `NEXT_API = PENDING_OWNER_HELD_406`)
9. STATE / ATTENTION / DECISIONS
10. INBOX (list CLI + read-only `GET /v1/inbox`; `INBOX_API = IMPLEMENTED`; listing != mutation != command)
11. API / CLI / WEB PRESENTATION

Honesty:

- DEMO_READINESS_IS_DERIVED = YES
- DEMO_READINESS_IS_AUTHORITY = NO
- MISSING != PASS
- DEMO_FIXTURE != AUTHENTIC_PILOT
- DEMO != RELEASE
- PILOT_READINESS = NOT_IMPLEMENTED
- RELEASE_READINESS = NOT_IMPLEMENTED

Does not copy owner-held #406 or #409.
