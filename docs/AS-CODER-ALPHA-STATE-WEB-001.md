# AS-CODER-ALPHA-STATE-WEB-001 — Web `#/state`

Package ID: `AS-CODER-ALPHA-STATE-WEB-001`.

Read-only production page for `GET /v1/state` (Coder Alpha current-state).
This is not the intelligence `/v1/project-state` page. Project is explicit
`?project=` only.

Honesty: `UI != CANONICAL` / `STATE != AUTHORITY` / `UNKNOWN != HEALTHY`.
