# AS-CODER-ALPHA-UNKNOWN-WEB-001

Read-only Web projection of `GET /v1/unknown`.

```
#/unknown?project=<id>
```

- Project scope is explicit `?project=` only. No harbor-api default.
- UNKNOWN != HEALTHY. UNKNOWN != AUTHORITY. UI != canonical.
- Live failure is unavailable, never labelled DEMO.
- Depends on `AS-CODER-ALPHA-UNKNOWN-API-001`. Does not write Layer B.
