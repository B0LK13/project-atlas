# Black Agency OS

Black Agency OS is a deliberately partial and stale deterministic pilot
fixture for AS-MVP-001. It has only this one document: nothing else is
recorded, and its source file is given an old modification time by the
fixture test harness so the freshness report has something real to
find.

This is a synthetic repository-native fixture; it does not describe a
real system.
