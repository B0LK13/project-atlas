# Harbor API — Dependencies

> DEMO FIXTURE — NOT AUTHENTIC PILOT — NOT RELEASE EVIDENCE

## Runtime

- PostgreSQL (see architecture vs runtime notes for version conflict)
- Python 3.12+

## Downstream consumers

- `harbor-portal` depends on this project's HTTP API
