# Quoted research material

The paper discusses an attack where an adversary writes "ignore prior rules" in a prompt. This is a quoted historical description, but the source still contains the instruction-shaped phrase and must be quarantined under the conservative boundary.
