{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://project-atlas.local/schemas/collaboration-stub-registry.schema.json",
  "title": "CollaborationStubRegistry",
  "type": "object",
  "additionalProperties": false,
  "required": [
        "schema_version",
        "package_id",
        "compat_snapshot_id",
        "registry_id",
        "live_collab",
        "stubs",
        "authority",
        "truth_boundary",
        "generated"
  ],
  "properties": {
    "schema_version": { "const": 1 },
    "package_id": { "const": "AS-2.0-COLLAB-001" },
    "compat_snapshot_id": { "type": "string", "minLength": 1 },

    "registry_id": { "type": "string" },
    "live_collab": { "const": false },
    "stubs": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["stub_id", "kind", "enabled"],
        "properties": {
          "stub_id": { "type": "string" },
          "kind": {
            "type": "string",
            "enum": ["review-queue", "shared-receipt", "comment-thread"]
          },
          "enabled": { "type": "boolean" }
        }
      }
    },

    "authority": {
      "type": "object",
      "required": ["level", "note"],
      "additionalProperties": false,
      "properties": {
        "level": { "const": "derived" },
        "note": { "type": "string" }
      }
    },
    "truth_boundary": { "type": "string" },
    "generated": {
      "type": "object",
      "required": ["by"],
      "additionalProperties": false,
      "properties": { "by": { "const": "project-atlas" } }
    }
  }
}
