# Offline session

Use the approved spool when the verified Vault is unavailable. Keep the logical Vault ID, UUID, project, agent, session, skill hash, event ID, and intended route in each event. Do not claim synchronized completion until replay succeeds.
