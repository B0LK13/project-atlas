"""Internal implementation modules for the atlas-vault-documentation skill.

These modules are imported by the CLI entry points under ``scripts/``.
They are standard-library only and contain no CLI concerns.
"""
