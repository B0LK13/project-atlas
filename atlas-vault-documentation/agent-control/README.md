# Agent Control Package

The importable implementation lives in `agent_control/` because Python module
names cannot contain hyphens. This directory preserves the documented control
plane boundary and points to the executable package and CLI scripts.
