# Relationship Authority Contract

Every Graphify relationship remains `authority: derived`. Verification may be verified, supported, inferred, conflicting or orphaned, but derived output cannot override source evidence.
