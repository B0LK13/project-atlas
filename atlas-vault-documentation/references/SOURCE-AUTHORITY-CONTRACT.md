# Source authority contract

Authority is explicit: `primary`, `maintained`, `derived`, or `external`.
Configured primary and derived paths take precedence over defaults. Derived
artifacts cannot override primary evidence; conflicts remain evidence-backed
records and are not automatically resolved.
