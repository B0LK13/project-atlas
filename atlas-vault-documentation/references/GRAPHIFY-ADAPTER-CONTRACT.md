# Graphify Adapter Contract

Only inventory-backed `graphify-output` records with matching SHA-256 hashes are accepted. Supported input is deterministic JSON/JSONL under the project root; unknown or malformed records are quarantined.
