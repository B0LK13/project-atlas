# Skill Distribution Contract

`skill/SKILL.md` and its manifest are canonical. Generated adapters contain the current hash and a do-not-edit warning. `verify-instructions` compares generated bytes with deterministic compiler output and fails on drift.
