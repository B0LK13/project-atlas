# Graph Validation Contract

Strict validation requires machine-readable graph state, deterministic JSONL stores, immutable receipts and router-owned Markdown projections. Quarantine is permitted only as explicit bounded state.
