# Supervised Agent Contract

Subagents either receive independent identities and receipts or operate as delegated workers whose parent records delegation, findings, changed files and validation. Invisible subagent work is not compliant.
