# Document classification contract

Classification is deterministic and records the selected type, confidence,
signals, competing candidates, and rule version. Ambiguous records remain
`unknown` or review-required. Graphify outputs are classified as derived and
semantic ingestion is deferred.
