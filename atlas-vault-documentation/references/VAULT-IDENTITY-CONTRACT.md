# Vault Identity Contract

Physical paths are resolved only after reading `.atlas/vault.json`. The logical Vault ID and UUID must match repository requirements; writable paths with the wrong identity are rejected.
