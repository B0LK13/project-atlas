# Agent Capability Contract

Adapters declare filesystem, shell, source-control, network and Atlas-command capabilities. An agent without the Atlas command surface is supervised or spool-only; it is not silently treated as independently compliant.
