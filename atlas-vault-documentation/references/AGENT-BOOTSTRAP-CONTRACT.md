# Agent Bootstrap Contract

Managed work starts through `atlas-agent run` or an equivalent registered launcher. Bootstrap verifies project configuration, logical Vault identity, canonical skill hash and spool availability before allocating a globally unique session.
