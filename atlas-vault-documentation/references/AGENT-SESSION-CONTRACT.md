# Agent Session Contract

Session state records agent, adapter, project, Vault, skill hash, milestone event IDs, pipeline counts and receipt state. Required events are session-start, validation and completion; strict postflight rejects missing events or pending spool.
