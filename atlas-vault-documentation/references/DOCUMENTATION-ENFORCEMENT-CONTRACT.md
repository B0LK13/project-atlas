# Documentation Enforcement Contract

Meaningful work uses `atlas-agent document`. Postflight requires session-start, validation and completion events plus a verified skill hash and receipt. Missing governance evidence returns `ATLAS SESSION INCOMPLETE` semantics and a non-zero exit.
