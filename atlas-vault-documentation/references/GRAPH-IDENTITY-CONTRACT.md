# Graph Identity Contract

Resolution precedence is explicit Atlas ID, configured mapping, then project-local stable identifier. Fuzzy matching is not used; unresolved or ambiguous records are quarantined.
