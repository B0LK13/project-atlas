# Architecture


## Integration

The client consumes the Project A HTTP JSON surface:

- Base URL (local): `http://127.0.0.1:8080`
- Auth: API key header
- Reads: `/v1/tenants`, `/v1/health`

## UI surfaces (synthetic)

- Tenant list
- Session status
