# Hidden holdouts — scoring only

These cases are **not** part of training or autolab path manifests.
`project_atlas.eval_substrate` fails closed if training/autolab roles resolve
any path under this directory.
